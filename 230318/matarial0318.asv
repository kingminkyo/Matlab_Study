clc, clear all

%재료역학 강의 메모 및 계산

tan = tan(0:0.01:(2*pi))';
plot(tan)
hold
sin = sin(0:0.01:(2*pi))';
plot(sin)
hold
co = sin(0:0.01:(2*pi))'

%기본적으로 각도 단위 rad에서 적용 됨

P = 30;
Q = 20;
th = 1/3*pi;
R = sqrt(P^2+Q^2 + (2*P*Q*cos(th)))

rx = 70 * cos(1/6*pi)-40*cosd(60)